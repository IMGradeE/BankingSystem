<!DOCTYPE html>
<html>

<head>
    <title>Report Results</title>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" type="text/css" href="css/bootstrap.min.css" />
    <link rel="stylesheet" type="text/css" href="font/bootstrap-icons.css" />
    <script src="./javascripts/selectPlaceholder.js"></script>

</head>

<body>
<div class="container-fluid vh-100" style="margin-top:10px">
    <div class="" style="margin-top:100px">
        <div class="rounded d-flex justify-content-center">
            <div class="col-lg-6 col-sm-12 shadow-lg p-5 bg-light">
                <div class="text-center">
                    <h3 class="text-primary">Results for <%=data%><!--TODO dynamically insert group name--></h3>
                </div>
                <div class="rounded d-flex flex-column justify-content-center align-items-center p-2">
                    <h2>Median time</h2>
                    <h5><%=median%> <!--TODO dynamically insert median time here --></h5>
                </div>
                <div class="rounded d-flex justify-content-center p-2">
                    <div class="m-1" style="width: 400px">
                        <table class="table table-striped table-hover">
                            <thead>
                            <tr>
                                <th colspan="2">Top 10 times</th>
                            </tr>
                            <tr>
                                <th scope="col">Username</th>
                                <th scope="col">Timing (ms)</th>
                            </tr>
                            </thead>
                            <tbody>
                            <!--TODO ejs code to populate table with 10 entries-->
                            <%if(Object.keys(dict).length){%>
                                <%for (var i = 0; i < Object.keys(dict).length; ++i){%>
                                    <tr>
                                        <td>
                                            <%=Object.keys(dict)[i];%>
                                        </td>
                                        <td>
                                            <%=Object.values(dict)[i];%>
                                        </td>
                                    </tr>
                                <%}%>
                            <%}%>
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<div class="rounded d-flex justify-content-center">
    <button type="button" class="btn btn-primary" onclick="window.location.href='/'">Return to Main</button>
</div>
</body>

</html>